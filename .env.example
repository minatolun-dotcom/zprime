POSTGRES_PASSWORD=zprime
JWT_SECRET=change-me-in-production
ADMIN_USER=admin
ADMIN_PASSWORD=admin123

# R-28 (optional): 32-byte base64 key encrypting IRP/EWB credentials at rest.
# Only required when configuring IRP connectivity (Company Settings).
# Generate: openssl rand -base64 32
IRP_ENC_KEY=
